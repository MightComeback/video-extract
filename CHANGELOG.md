# Changelog

## v0.1 (2026-01-31)

- Initial MVP: `fathom2action` CLI + `fathom2action-extract` / `fathom2action-transform` micro-tools.
- Best-effort URL fetch + HTML → text normalization; fallback to `--stdin` for private/auth-gated links.
- Deterministic markdown bug brief template output.
